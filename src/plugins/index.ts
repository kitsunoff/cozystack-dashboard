/**
 * Plugin loader — import this file to activate all internal plugins.
 * Each plugin registers its components via the registry system.
 */

import "./core";
import "./kubernetes";
import "./vm-instance";
import "./vm-disk";
