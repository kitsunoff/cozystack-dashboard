apiVersion: v1
kind: Service
metadata:
  name: cozy-dashboard-web
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app.kubernetes.io/instance: cozy-dashboard
    app.kubernetes.io/name: web
